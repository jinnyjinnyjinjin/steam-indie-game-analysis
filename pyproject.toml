[project]
name = "game-analysis"
version = "0.1.0"
description = "steam indie game analysis project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary",
    "python-dotenv",
]
